import QuoteEditor from "@/components/QuoteEditor";

export default function NewQuotePage() {
    return (
        <div>
            <h1 style={{ marginBottom: '2rem', color: 'var(--text-main)' }}>Nuevo Presupuesto</h1>
            <QuoteEditor />
        </div>
    );
}
